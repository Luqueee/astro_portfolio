---
interface Props {
  link: string;
  target: string;
}

const { link, target = '' } = Astro.props;
---

<a
  target={target}
  class="w-32 h-12 flex justify-center align-middle bg-white rounded-md hover:scale-105 transition-all"
  href={link}>
  <slot />
</a>
