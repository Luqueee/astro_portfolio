---
import '@fontsource/exo-2/700.css'; // Defaults to weight 400

interface Props {
  img: string;
}

const { img } = Astro.props;

import python from './langs/python.astro';

const TAGS = {
  PYTHON: {
    name: 'Python',
    class: 'bg-yellow-700 text-white',
    icon: python,
  },
};

const PROJECTS = [
  {
    title: 'App de facturas con Tkinter',
    description:
      'Una app de facturas hecha en Python que permite gestionar el inventario, crear clientes, generar presupuestos y calcularlos, ademas de que permite generar facturas professionales automatizadas con todos los datos del cliente.',
    link: 'https://svgl.vercel.app/',
    github: 'https://github.com/pheralb/svgl',
    image: '/projects/bill.png',
    tags: [TAGS.PYTHON],
  },
];
---

<div class="flex flex-col gap-y-16">
  {
    PROJECTS.map(({ image, title, description, tags, link, github }) => (
      <article class="flex flex-col space-x-0 space-y-8 group md:flex-row md:space-x-8 md:space-y-0">
        <div class="w-full md:w-1/2">
          <div class="relative flex flex-col items-center col-span-6 row-span-5 gap-8 transition duration-500 ease-in-out transform shadow-xl overflow-clip rounded-xl sm:rounded-xl md:group-hover:-translate-y-1 md:group-hover:shadow-2xl lg:border lg:border-gray-800 lg:hover:border-gray-700 lg:hover:bg-gray-800/50">
            <img
              alt="Recién llegado vs 5 años en Nueva Zelanda"
              class="object-cover object-top w-full h-56 transition duration-500 sm:h-full md:scale-110 md:group-hover:scale-105"
              loading="lazy"
              src={image}
            />
          </div>
        </div>

        <div class="w-full md:w-1/2 md:max-w-lg">
          <h3 class="text-2xl font-bold text-white-800 dark:text-gray-100">
            {title}
          </h3>
          <div class="flex flex-wrap mt-2">
            <ul class="flex flex-row mb-2 gap-x-2">
              {tags.map((tag) => (
                <li>
                  <span
                    class={`flex gap-x-2 rounded-full text-xs ${tag.class} w-42`}>
                    <tag.icon class="" />
                    <p class="mt-auto mb-auto mr-4 ">{tag.name}</p>
                  </span>
                </li>
              ))}
            </ul>

            <div class="mt-2 dark:text-gray-400">{description}</div>
          </div>
        </div>
      </article>
    ))
  }
</div>
